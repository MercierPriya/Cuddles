<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <style>
 @import url('https://fonts.googleapis.com/css2?family=Aclonica&family=Cambo&family=Lily+Script+One&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Shantell+Sans:ital,wght@0,300..800;1,300..800&display=swap');

*{
    margin: 0%;
    padding: 0%;
    box-sizing: border-box;
}
        :root {
    --main-color: #FF9A8B;  /* Warm Peach */
    --accent-color: #E76F51; /* Soft Coral */
    --bg-color: #FAF3E0;    /* Creamy White */
    --text-color: #2A2A2A; 
    --main-color2:#14213D; /* Dark Gray */
  }
  .lily-script-one-regular {
    font-family: "Lily Script One", serif;
    font-weight: 400;
    font-size:32px;
    font-style: normal;
  }
  
.navbar{
    background-color:var(--main-color) !important;
}

.nav-link {
    color: var(--text-color) !important;
    margin-right: 20px;
    font-size: 1.1rem;
    font-weight: 500;
    padding: 8px 12px;
    transition: color 0.3s ease-in-out;
}

.nav-link::after {
    left: 40%;
    bottom: 0;
    width: 0%;
    height: 2px;
    transition: width 0.4s ease-in-out, left 0.4s ease-in-out;
}

.nav-link:hover {
color: var(--bg-color) !important;
transform:scaleY(1) ;
}

.navbar-brand img:hover{
    transition: 0.5sec;
    transform: scaleY(1);
}
 .navbar-brand:hover{ 
    transition: 0.3sec  ease-in-out;
    color: var(--text-color);
    transform: scale(1.1);}
.navbar-brand img:hover{
    transition: 0.5sec;
    transform: scaleY(1);
}
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <!-- Logo et Nom à gauche -->
            <a class="navbar-brand d-flex align-items-center" href="/">
                <img id="logo" src="/images/logo-color.png" alt="logo" style="width:50px; border-radius:10px">
                <span class="ms-2  lily-script-one-regular">Cuddles</span>
            </a>
    
            <!-- Bouton pour Mobile -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav align-items-center">
                    <!-- Icône de Notification -->
                    <li class="nav-item">
                        <a class="nav-link position-relative" href="/parent-notifications">
                            <i class="fas fa-bell fa-lg"></i>
                            <% if (typeof unreadNotificationCount !== 'undefined' && unreadNotificationCount > 0) { %>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                 <%= unreadNotificationCount %><!-- Nombre de notifications dynamiques -->
                            </span>
                            <% } %>
                        </a>
                    </li>
    
                    <!-- Menu déroulant Profil -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                            <img src="/uploads/<%= user.profileImage || 'default.jpg' %>" alt="Profil" class="rounded-circle" width="50" height="50">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="/parent-profile">Profil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/logout">Déconnexion</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</body>
</html>