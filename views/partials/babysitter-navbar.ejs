<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= babysitter.name %>-notification</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
 <style>
   
        @import url('https://fonts.googleapis.com/css2?family=Aclonica&family=Cambo&family=Lily+Script+One&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Shantell+Sans:ital,wght@0,300..800;1,300..800&display=swap');

        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
        }

        :root {
            --main-color: #FF9A8B;  /* Warm Peach */
            --accent-color: #E76F51; /* Soft Coral */
            --bg-color: #FAF3E0;    /* Creamy White */
            --text-color: #2A2A2A; 
            --main-color2: #14213D; /* Dark Gray */
        }

        .lily-script-one-regular {
            font-family: "Lily Script One", serif;
            font-weight: 400;
            font-size: 32px;
            font-style: normal;
        }

        .navbar {
            background-color: var(--main-color2)!important;
        }

        .nav-link {
            color: var(--bg-color);
            margin-right: 20px;
            font-size: 1.1rem;
            font-weight: 500;
            padding: 8px 12px;
            transition: color 0.3s ease-in-out;
        }

        .nav-link:hover {
            color: var(--main-color);
            transform: scaleY(1);
        }

        .navbar-brand {
            color: var(--bg-color);
        }

        .navbar-brand img {
            transition: 0.5s;
            transform: scaleY(1);
        }

        .navbar-brand:hover { 
            transition: 0.3s ease-in-out;
            color: var(--bg-color);
            transform: scale(1.1);
        }

        .navbar-toggler img {
            transition: 0.5s;
            border-radius: 50%;
        }
        .custom-toggler {
    border: none;
    background-color:var(--bg-color);
    color:var(--accent-color);
    font-size: 24px;
}

        .navbar-toggler img:hover {
            transform: scale(1.1);
        }
        @media screen and (max-width: 600px) {
   
    .navbar-brand span {
        display:none;
    }
}
 
 </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <img id="logo" src="/images/logo-color.png" alt="logo" style="width:50px; border-radius:10px">
            <span class="ms-2 lily-script-one-regular">Cuddles</span>
        </a>

        <!-- Mobile Toggle Button -->
        <button class="navbar-toggler  custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <!-- <img src="/uploads/babysitter.profileImage || 'default-avatar.png'%>" alt="Profil" class="rounded-circle" width="50" height="50"> -->
         <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav align-items-center">
                <!-- Notification Bell -->
                <li class="nav-item">
                    <a class="nav-link position-relative" href="/babysitter-notification">
                        <i class="fas fa-bell fa-lg"></i>
                        <!-- if (typeof reservationsCount !== 'undefined' && reservationsCount > 0) { -->
                           <% if (unreadCount > 0) { %>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"><%= unreadCount %></span>
                        <% } %>
                    </a>
                </li>

                <!-- Profile Menu -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="/babysitter-profile" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <img src="/uploads/<%= babysitter.profileImage || 'default.jpg' %>?v=<%= Date.now() %>" alt="Profil" class="rounded-circle" width="50" height="50">
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        <li class="dropdown-item">
                            <strong>Status:</strong> 
                            <% if (babysitter.status === 'approved') { %>
                               <a href="/babysitter-profile" style="text-decoration: none;"><span class="text-success">✅ Profil vérifié</span></a> 
                            <% } else { %>
                                <span class="text-warning">⚠️ En cours de vérification</span>
                            <% } %>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item text-danger" href="/logout">Déconnexion</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
</body>